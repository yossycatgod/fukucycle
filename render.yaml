services:
  - type: web
    name: fukucycle
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python create_test_account.py
    startCommand: python serve.py
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.7
